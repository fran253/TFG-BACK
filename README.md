# TFG-BACK


# Rama NUBE 
desplegada web con kubernetes en la nube

# Rama Develop 
desplegar en local


# /IA 
carpeta con todo lo relacionado con la IA de mistral, para pruebas en local avisadnos y crearemos la key desde open router y os pasaremos la clave para ponerla dentro del fichero .env

# Docker
docker compose up --build -d
